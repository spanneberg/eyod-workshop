---

:backends:
  - yaml
:yaml:
  :datadir: "/opt/puppet"
:hierarchy:
  - "environments/%{::environment}/hieradata/nodes/%{::fqdn}"
  - "environments/%{::environment}/hieradata/nodes/%{::hostname}"
  - "environments/%{::environment}/hieradata/common"
  - "hieradata/common"
